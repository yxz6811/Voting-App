## 1. 身份存储与校验

- [x] 1.1 定义当前用户类型（姓名、学号）与 `localStorage` 键名，实现读取/写入/清除封装
- [x] 1.2 实现与设计一致的校验：姓名 trim 后非空且 ≤32 码点；学号 `^[0-9A-Za-z]{4,20}$`
- [x] 1.3 应用启动时从 `localStorage` 恢复登录态；数据损坏或校验失败时回退为未登录且不抛未处理异常

## 2. 登录与登出 UI

- [x] 2.1 实现登录界面：姓名、学号输入与提交；非法输入时展示明确错误提示且不调登录成功路径
- [x] 2.2 登录成功后持久化身份并进入已登录 UI 流程
- [x] 2.3 在全局布局（或等价位置）展示当前用户姓名，并提供登出操作；登出后清除存储并回到未登录态

## 3. 集成与验收

- [x] 3.1 按 `specs/user-login/spec.md` 自测：登录、非法输入拒绝、登出、刷新后仍登录、登出后刷新仍为未登录
- [x] 3.2 为后续「上传/投票」预留读取当前用户的 API（如 context/hook），避免页面各自读存储
