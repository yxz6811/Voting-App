## 1. 数据层（IndexedDB）

- [x] 1.1 定义提交记录类型（`submissionId`、`createdAt`、作者快照、`mimeType`、`originalFileName`、`byteSize`、媒体种类）与 DB 名称/版本策略
- [x] 1.2 实现打开数据库、写入一条提交（Blob + 元数据）、列出全部并按 `createdAt` 降序读取
- [x] 1.3 对存储失败（如配额）捕获并向上层返回可读错误，不抛未处理异常

## 2. 校验与业务规则

- [x] 2.1 实现 80 MiB 上限与 `design.md` 中的 MIME/扩展名白名单（含 `File.type` 为空时的扩展名回退）
- [x] 2.2 实现提交 API：仅在 `useAuth().user` 存在时允许调用；成功时写入快照作者字段

## 3. UI 与集成

- [x] 3.1 在已登录布局中增加「上传作品」与「班级列表」入口；未登录时不得出现可成功提交的控件
- [x] 3.2 上传区：文件选择、校验错误提示、提交中状态、成功后列表即时更新
- [x] 3.3 班级列表：展示作者姓名、媒体类型；图片 `<img>`、视频 `<video>` 预览；监听 `error` 展示可见失败态
- [x] 3.4 管理 `createObjectURL` 生命周期（列表更新/卸载时 `revokeObjectURL`），避免泄漏

## 4. 验收

- [x] 4.1 按 `specs/class-submissions/spec.md` 自测：未登录不可传、类型/大小拒绝、成功持久化与刷新后仍在、排序新在前、视频不可播时有可见错误态
- [x] 4.2 `npm run build` 通过；必要时补充轻量单元测试（纯校验函数）或文档化手工步骤
